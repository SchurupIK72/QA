# QA Automation - Команды запуска скриптов

## 🔄 Swagger to Postman Generator

Интерактивный генератор коллекций Postman из Swagger/OpenAPI JSON.

### Запуск:

```powershell
# С указанием файла
python scripts/swagger_to_postman.py путь/к/swagger.json

# Интерактивный режим (запросит путь)
python scripts/swagger_to_postman.py
```

### Пример:

```powershell
python scripts/swagger_to_postman.py confidential/swagger.json
```

### Возможности:
- ✅ Парсинг Swagger/OpenAPI 3.0
- ✅ Интерактивный ввод учетных данных (user, admin, custom)
- ✅ Автоматическое извлечение токенов из ответов SignIn
- ✅ Извлечение ID созданных сущностей
- ✅ Кастомные извлечения любых полей
- ✅ Группировка запросов по тегам/контроллерам
- ✅ Генерация pre-request и test скриптов

---

## 🎲 Генератор тестовых данных

```powershell
# Интерактивный режим (рекомендуется)
python scripts/test_data_generator.py
```

**Возможности:**
- Генерация пользователей, персонажей, email, паролей, телефонов, цен, дат
- Экспорт в JSON/CSV
- Файлы сохраняются в папку `TestData/`

---

## 📄 Конвертер DOCX → Markdown (Pandoc)

### Установка Pandoc (один раз, от имени Администратора):
```powershell
choco install pandoc
```

### Запуск:
```powershell
# Интерактивный режим (рекомендуется)
python scripts/docx_to_md.py

# Конвертация конкретного файла
python scripts/docx_to_md.py "путь/к/файлу.docx"

# С указанием выходного файла
python scripts/docx_to_md.py "input.docx" "output.md"
```

**Возможности:**
- Интерактивный режим с выбором файла из списка
- Пакетная конвертация всех DOCX в папке
- Извлечение изображений в папку `media/`
- Статистика: строки, слова, символы

---

## 📁 Быстрое создание проекта

```powershell
# Создать структуру нового проекта
$projectName = "НазваниеПроекта"
mkdir "projects/$projectName"
mkdir "projects/$projectName/bug-reports"
mkdir "projects/$projectName/checklists"
mkdir "projects/$projectName/test-data"
mkdir "projects/$projectName/docs"

# Скопировать шаблоны
Copy-Item "templates/*.csv" "projects/$projectName/checklists/"
```

---

## 🔧 Полезные команды

### Работа с Git:
```powershell
# Статус репозитория
git status

# Добавить все изменения
git add .

# Коммит
git commit -m "Описание изменений"

# Пуш на GitHub
git push origin main
```

### Очистка TestData:
```powershell
# Удалить сгенерированные файлы
Remove-Item TestData/*.json -ErrorAction SilentlyContinue
Remove-Item TestData/*.csv -ErrorAction SilentlyContinue
```

### Поиск файлов:
```powershell
# Найти все CSV файлы
Get-ChildItem -Recurse -Filter "*.csv"

# Найти все DOCX файлы
Get-ChildItem -Recurse -Filter "*.docx"

# Найти файлы по маске
Get-ChildItem -Recurse -Filter "*bug*"
```

---

## 📋 Установка зависимостей

```powershell
# Pandoc (для конвертации DOCX → Markdown)
# Запустить PowerShell от имени Администратора!
choco install pandoc

# Проверить установку Pandoc
pandoc --version
```

---

## 🎭 Генератор Playwright тестов

### Установка зависимостей (один раз):
```powershell
pip install pytest playwright
playwright install
```

### Запуск генератора:
```powershell
# Интерактивный режим
python scripts/playwright_generator.py
```

### Запуск сгенерированных тестов:
```powershell
# Все тесты
pytest tests/ -v

# С отображением браузера
pytest tests/ -v --headed

# Конкретный браузер (chromium, firefox, webkit)
pytest tests/ -v --browser firefox

# Конкретный класс тестов
pytest tests/ -v -k "TestNavigation"

# Только быстрые тесты
pytest tests/ -v -m "not slow"
```

**Возможности генератора:**
- Тесты навигации по разделам сайта
- Тесты header (логотип, меню, поиск, sticky)
- Тесты hero-секции (карусель, видео)
- Тесты поиска
- Тесты форм обратной связи
- Тесты футера
- Тесты cookie-баннера
- Тесты страницы 404
- Тесты адаптивности (мобильные устройства)
- Тесты производительности
- Тесты SEO

---

## 🚀 Быстрый старт

```powershell
# 1. Перейти в папку проекта
cd C:\REPO\QArepo\QA

# 2. Запустить генератор тестовых данных
python scripts/test_data_generator.py

# 3. Или конвертировать DOCX в Markdown
python scripts/docx_to_md.py
```
